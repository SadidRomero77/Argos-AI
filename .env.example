# Copiar a .env y rellenar. .env NUNCA se sube a git.
#
#   cp .env.example .env

# Requerido salvo en modo ARGOS_MODELS__LOCAL_ONLY=true
ANTHROPIC_API_KEY=

# Opcional — Fase 2, visión espacial (gemini-robotics-er-1.6-preview)
GEMINI_API_KEY=

# Fase 3 — clave precompartida del bus entre laptop, Pi y ESP32.
# Generar con:  python -c "import secrets; print(secrets.token_urlsafe(32))"
ARGOS_BUS_KEY=
